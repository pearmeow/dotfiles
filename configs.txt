Good stuff
window manager: i3
taskbar: i3bar (temp)

Minimal applications
terminal emulator: alacritty
file manager: thunar
text editor: neovim

Other components usually provided by desktop environments:

Desktop compositor (Xorg) : picom (config later)
Notification daemon: dunst (config later)
Display manager: sddm (config later)
Screen locker: i3 (change later)
Application launcher: rofi (config later)
Audio control: alsamixer (manual launch from terminal)
Polkit authentication agent: polkit-kde-agent (exec --no-startup-id)
Screen capture: flameshot (exec --no-startup-id))
Wallpaper setter: feh (exec --no-startup-id ~/.fehbg &)
Logout dialogue: i3 (change later)

Default applications:
clipmenu (CM_LAUNCHER=rofi)

Seemingly unnecessary stuff:
Backlight control:
Media control:
Power management: powertop
Screen temperature:

Extra:
conky
